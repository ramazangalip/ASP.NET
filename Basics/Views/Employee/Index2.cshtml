@model String[]
<div>
    <h3 class="display-3">
        Hello World
    </h3>
    <div class="lead">
        @foreach (string name in @Model)
        {
            <p>@name</p>
        }
    </div>
</div>